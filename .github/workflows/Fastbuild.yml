name: 🚀 Android Fast Build

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

env:
  GRADLE_OPTS: -Dorg.gradle.daemon=false -Dorg.gradle.workers.max=2 -Dorg.gradle.parallel=true -Xmx4g
  JAVA_OPTS: -Xmx4g

jobs:
  # ═════════════════════════════════════════════════════════════
  #  🎫 JOB 1 : TESTS CRITIQUES
  # ═════════════════════════════════════════════════════════════
  critical-tests:
    name: 🧪 Critical Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: ☕ Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: 🤖 Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: 🔧 Fix Gradlew Permissions
        run: |
          chmod +x gradlew
          sed -i 's/\r$//' gradlew

      # ✅ FIX 1: Clean avant compilation pour forcer rebuild
      - name: 🧹 Clean Build
        run: ./gradlew clean --no-daemon

      # ✅ FIX 2: Incrémente versionCode avec le numéro de run
      - name: 🔢 Update Version Code
        run: |
          # Extraire versionName actuelle
          VERSION_NAME=$(grep "versionName" app/build.gradle.kts | awk '{print $2}' | tr -d '"')
          # Créer un versionCode unique basé sur le timestamp + run number
          TIMESTAMP=$(date +%s)
          VERSION_CODE=$((GITHUB_RUN_NUMBER + TIMESTAMP))
          
          # Remplacer dans build.gradle.kts (ou .gradle)
          sed -i "s/versionCode = [0-9]*/versionCode = ${VERSION_CODE}/" app/build.gradle.kts
          sed -i "s/versionName = \"[^\"]*\"/versionName = \"${VERSION_NAME}-SNAPSHOT-${GITHUB_RUN_NUMBER}\"/" app/build.gradle.kts
          
          echo "✅ Version updated to: ${VERSION_NAME}-SNAPSHOT-${GITHUB_RUN_NUMBER} (code: ${VERSION_CODE})"

      - name: 🔨 Compile & Test
        run: ./gradlew assembleDebug testDebugUnitTest --no-daemon --stacktrace

      - name: 🔍 Run Lint
        run: ./gradlew lintDebug --no-daemon --stacktrace
        continue-on-error: true

      - name: 📊 Upload Test Reports (if failed)
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: test-reports-${{ github.sha }}
          path: |
            app/build/reports/tests/
            app/build/reports/lint-results*.html
          retention-days: 3

  # ═════════════════════════════════════════════════════════════
  #  🎫 JOB 2 : BUILD DEBUG APK
  # ═════════════════════════════════════════════════════════════
  build-debug:
    name: 📦 Build Debug APK
    runs-on: ubuntu-latest
    needs: critical-tests
    timeout-minutes: 30
    
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: ☕ Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: 🤖 Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: 🔧 Fix Gradlew Permissions
        run: |
          chmod +x gradlew
          sed -i 's/\r$//' gradlew

      # ✅ FIX 3: Clean et incrément version (re-run car checkout fresh)
      - name: 🧹 Clean Build
        run: ./gradlew clean --no-daemon

      - name: 🔢 Update Version Code
        run: |
          VERSION_NAME=$(grep "versionName" app/build.gradle.kts | awk '{print $2}' | tr -d '"')
          TIMESTAMP=$(date +%s)
          VERSION_CODE=$((GITHUB_RUN_NUMBER + TIMESTAMP))
          sed -i "s/versionCode = [0-9]*/versionCode = ${VERSION_CODE}/" app/build.gradle.kts
          sed -i "s/versionName = \"[^\"]*\"/versionName = \"${VERSION_NAME}-SNAPSHOT-${GITHUB_RUN_NUMBER}\"/" app/build.gradle.kts
          echo "✅ Version updated to: ${VERSION_NAME}-SNAPSHOT-${GITHUB_RUN_NUMBER} (code: ${VERSION_CODE})"

      # 🎯 Build avec rebuild forcé
      - name: 📦 Build Debug APK
        run: ./gradlew assembleDebug --no-daemon --stacktrace --rerun-tasks

      # ✅ FIX 4: Renommer l'APK avec timestamp pour garantir unicité
      - name: 🏷️ Rename APK
        run: |
          cd app/build/outputs/apk/debug
          TIMESTAMP=$(date +%Y%m%d_%H%M%S)
          mv app-debug.apk app-debug-${{ github.run_number }}-${TIMESTAMP}.apk
          echo "✅ APK renamed with timestamp"

      - name: 🚀 Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-${{ github.sha }}-${{ github.run_number }}
          path: app/build/outputs/apk/debug/*.apk
          retention-days: 7

  # ═════════════════════════════════════════════════════════════
  #  🎫 JOB 3 : FINAL REPORT
  # ═════════════════════════════════════════════════════════════
  final-report:
    name: ✅ Final Report
    runs-on: ubuntu-latest
    needs: [critical-tests, build-debug]
    if: always()
    
    steps:
      - name: 🎉 Build Status
        run: |
          echo "Tests: ${{ needs.critical-tests.result }}"
          echo "Debug APK: ${{ needs.build-debug.result }}"
